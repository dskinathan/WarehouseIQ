// Placeholder entry point. M0 is schema/RLS/auth foundations only — the
// real Manager Dashboard screens (docs/product/SCREENS.md) land in M1.
export default function Home() {
  return (
    <main>
      <h1>WarehouseIQ</h1>
      <p>Manager Dashboard — screens land in M1. See docs/product/SCREENS.md.</p>
    </main>
  );
}
